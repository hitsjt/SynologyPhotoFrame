<UserControl x:Class="SynologyPhotoFrame.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="600">

    <Grid Background="{StaticResource PrimaryDarkBrush}">
        <Border Background="{StaticResource SecondaryDarkBrush}"
                CornerRadius="16"
                MaxWidth="500" MaxHeight="650"
                HorizontalAlignment="Center" VerticalAlignment="Center"
                Padding="40"
                BorderBrush="#E2E8F0" BorderThickness="1">
            <StackPanel>
                <!-- Title -->
                <TextBlock Text="Synology Photo Frame"
                           FontSize="28" FontWeight="Bold"
                           Foreground="{StaticResource TextPrimaryBrush}"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,8"/>
                <TextBlock Text="Connect to your NAS"
                           FontSize="16"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,32"/>

                <!-- NAS Address -->
                <TextBlock Text="NAS Address" Foreground="{StaticResource TextSecondaryBrush}"
                           FontSize="14" Margin="0,0,0,4"/>
                <TextBox Text="{Binding NasAddress, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,0,0,16"/>

                <!-- Port and HTTPS -->
                <Grid Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Margin="0,0,16,0">
                        <TextBlock Text="Port" Foreground="{StaticResource TextSecondaryBrush}"
                                   FontSize="14" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding Port, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                    <CheckBox Grid.Column="1" Content="HTTPS"
                              IsChecked="{Binding UseHttps}"
                              VerticalAlignment="Bottom" Margin="0,0,0,4"/>
                </Grid>

                <!-- Username -->
                <TextBlock Text="Username" Foreground="{StaticResource TextSecondaryBrush}"
                           FontSize="14" Margin="0,0,0,4"/>
                <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,0,0,16"/>

                <!-- Password -->
                <TextBlock Text="Password" Foreground="{StaticResource TextSecondaryBrush}"
                           FontSize="14" Margin="0,0,0,4"/>
                <PasswordBox x:Name="PasswordBox"
                             PasswordChanged="PasswordBox_PasswordChanged"
                             Margin="0,0,0,16"/>

                <!-- Remember Me -->
                <CheckBox Content="Remember me"
                          IsChecked="{Binding RememberMe}"
                          Margin="0,0,0,24"/>

                <!-- Error Message -->
                <TextBlock Text="{Binding ErrorMessage}"
                           Foreground="#ff4444" FontSize="14"
                           TextWrapping="Wrap"
                           Margin="0,0,0,16"
                           Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibilityConverter}}"/>

                <!-- Connect Button -->
                <Button Content="{Binding IsConnecting, Converter={StaticResource BoolToConnectTextConverter}}"
                        Command="{Binding ConnectCommand}"
                        Style="{StaticResource PrimaryButton}"
                        HorizontalAlignment="Stretch"
                        IsEnabled="{Binding IsConnecting, Converter={StaticResource InverseBoolConverter}}"/>
            </StackPanel>
        </Border>

        <!-- Loading Overlay -->
        <Grid Background="#80F0F4F8"
              Visibility="{Binding IsConnecting, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="Connecting..." FontSize="20" Foreground="{StaticResource TextPrimaryBrush}"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
